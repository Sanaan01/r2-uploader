# R2 Upload API Configuration
# These settings connect to your Cloudflare Worker backend

# Worker API URL (local dev or production)
VITE_UPLOAD_API_URL=http://localhost:8787

# Upload API Key (must match UPLOAD_API_KEY secret in worker)
# Generate a secure key: openssl rand -base64 32
VITE_UPLOAD_API_KEY=your-secret-upload-key-here
