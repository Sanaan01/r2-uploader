# R2 Upload API Configuration
# These settings connect to your Cloudflare Worker backend

# Worker API URL (local dev or production)
VITE_UPLOAD_API_URL=http://localhost:8787

# Upload API Key (must match UPLOAD_API_KEY secret in worker)
VITE_UPLOAD_API_KEY=your-secret-key-here

# Site Password Protection
# Generate hash: enter password in browser console, run:
# let hash=0;for(let i=0;i<"yourpassword".length;i++){hash=((hash<<5)-hash)+"yourpassword".charCodeAt(i);hash=hash&hash}hash.toString(36)
VITE_PASSWORD_HASH=your-hash-here
